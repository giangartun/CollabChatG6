/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,ts,jsx,tsx}" // 👈 incluye todos tus archivos TS/TSX
  ],
  theme: {
    extend: {}, // aquí podés personalizar colores, fuentes, etc. si querés
  },
  plugins: [],
}
